# 🚀 Пошаговая инструкция деплоя UseMyTime на Railway

## Предварительные требования
- ✅ Аккаунт на GitHub (у вас уже есть)
- ✅ Проект залит на GitHub (уже сделано)
- ✅ Код готов к деплою (уже настроен)

---

## Шаг 1: Регистрация на Railway

### 1.1 Откройте Railway
Перейдите на сайт: **https://railway.app**

### 1.2 Войдите через GitHub
1. Нажмите кнопку **"Login"** в правом верхнем углу
2. Выберите **"Login with GitHub"**
3. Авторизуйте Railway доступ к вашему GitHub аккаунту
4. Подтвердите доступ к репозиториям

---

## Шаг 2: Создание нового проекта

### 2.1 Создайте проект
1. После входа нажмите **"New Project"** (большая фиолетовая кнопка)
2. Выберите **"Deploy from GitHub repo"**

### 2.2 Выберите репозиторий
1. В списке найдите репозиторий **"UseMyTime"** или **"newvirinin/UseMyTime"**
2. Нажмите на него
3. Railway спросит, какую ветку использовать
4. Выберите ветку **"UseMyTime"** (или main, если вы смержили изменения)

### 2.3 Подождите первоначальной сборки
Railway автоматически начнет деплой. Пока НЕ ПАНИКУЙТЕ, если он упадет с ошибкой - это нормально, нам нужно настроить базу данных.

---

## Шаг 3: Добавление PostgreSQL базы данных

### 3.1 Добавьте базу данных
1. В вашем проекте нажмите кнопку **"New"** (справа вверху)
2. Выберите **"Database"**
3. Выберите **"Add PostgreSQL"**
4. Railway создаст базу данных и автоматически добавит переменные окружения

### 3.2 Проверьте переменные базы данных
1. Кликните на сервис PostgreSQL (появится в проекте)
2. Перейдите на вкладку **"Variables"**
3. Вы увидите автоматически созданные переменные:
   - `PGHOST`
   - `PGPORT`
   - `PGDATABASE`
   - `PGUSER`
   - `PGPASSWORD`
   - `DATABASE_URL`

---

## Шаг 4: Настройка переменных окружения для Django

### 4.1 Откройте настройки вашего Django сервиса
1. Вернитесь к списку сервисов (нажмите на название проекта вверху)
2. Кликните на ваш Django сервис (UseMyTime)
3. Перейдите на вкладку **"Variables"**

### 4.2 Добавьте обязательные переменные
Нажмите **"New Variable"** и добавьте каждую переменную:

#### Обязательные переменные:

**1. SECRET_KEY**
```
SECRET_KEY
```
Значение: Сгенерируйте новый ключ. Можно использовать:
```python
# В Python консоли:
from django.core.management.utils import get_random_secret_key
print(get_random_secret_key())
```
Или просто используйте длинную случайную строку (50+ символов)

**2. DEBUG**
```
DEBUG
False
```

**3. DB_ENGINE**
```
DB_ENGINE
postgresql
```

**4. DB_HOST** (используйте переменную из PostgreSQL)
```
DB_HOST
${{Postgres.PGHOST}}
```
⚠️ Важно: Замените `Postgres` на точное имя вашего PostgreSQL сервиса в Railway

**5. DB_PORT**
```
DB_PORT
${{Postgres.PGPORT}}
```

**6. DB_NAME**
```
DB_NAME
${{Postgres.PGDATABASE}}
```

**7. DB_USER**
```
DB_USER
${{Postgres.PGUSER}}
```

**8. DB_PASSWORD**
```
DB_PASSWORD
${{Postgres.PGPASSWORD}}
```

### 4.3 Настройте домен (сначала получите его)

Пока пропустите этот шаг, вернемся к нему после первого успешного деплоя.

---

## Шаг 5: Перезапуск деплоя

### 5.1 Запустите новый деплой
1. Перейдите на вкладку **"Deployments"**
2. Нажмите **"Deploy"** → **"Redeploy"**
3. Или просто сделайте любой коммит в GitHub - Railway автоматически задеплоит

### 5.2 Следите за логами
1. На вкладке **"Deployments"** выберите текущий деплой
2. Нажмите **"View Logs"**
3. Следите за процессом:
   - Установка зависимостей
   - Сборка статических файлов (`collectstatic`)
   - Применение миграций (`migrate`)
   - Запуск Gunicorn

### 5.3 Дождитесь успешного деплоя
Когда увидите:
```
✓ Build successful
✓ Deployment successful
```
Переходите к следующему шагу.

---

## Шаг 6: Получение и настройка домена

### 6.1 Получите домен Railway
1. Перейдите на вкладку **"Settings"**
2. Прокрутите до раздела **"Domains"**
3. Нажмите **"Generate Domain"**
4. Railway создаст домен вида: `usemytime-production.up.railway.app`
5. Скопируйте этот домен

### 6.2 Добавьте домен в переменные окружения
1. Вернитесь на вкладку **"Variables"**
2. Добавьте две новые переменные:

**ALLOWED_HOSTS**
```
ALLOWED_HOSTS
usemytime-production.up.railway.app
```
⚠️ Замените на ваш реальный домен!

**CSRF_TRUSTED_ORIGINS**
```
CSRF_TRUSTED_ORIGINS
https://usemytime-production.up.railway.app
```
⚠️ Замените на ваш реальный домен! Обратите внимание на `https://`

### 6.3 Перезапустите сервис
После добавления переменных Railway автоматически перезапустит сервис.

---

## Шаг 7: Создание суперпользователя

### 7.1 Установите Railway CLI
Откройте терминал на вашем компьютере:

```bash
npm install -g @railway/cli
```

Если у вас нет npm, установите Node.js с https://nodejs.org

### 7.2 Войдите в Railway
```bash
railway login
```
Откроется браузер для авторизации.

### 7.3 Подключитесь к проекту
```bash
cd /path/to/your/UseMyTime
railway link
```
Выберите ваш проект из списка.

### 7.4 Создайте суперпользователя
```bash
railway run python UseMyTime/manage.py createsuperuser
```

Введите:
- Username (имя пользователя)
- Email
- Password (дважды)

---

## Шаг 8: Проверка работы сайта

### 8.1 Откройте ваш сайт
Перейдите по адресу: `https://ваш-домен.up.railway.app`

### 8.2 Проверьте главную страницу
Должна открыться главная страница UseMyTime.

### 8.3 Проверьте админку
Перейдите: `https://ваш-домен.up.railway.app/admin/`

Войдите с учетными данными суперпользователя.

### 8.4 Проверьте статические файлы
Убедитесь, что CSS и изображения загружаются корректно.

---

## Шаг 9: Настройка Email (опционально)

Если хотите, чтобы работала отправка email (восстановление пароля и т.д.):

### 9.1 Добавьте переменные Email
В разделе **"Variables"** добавьте:

```
EMAIL_HOST
smtp.yandex.com
```

```
EMAIL_HOST_USER
ваш-email@yandex.ru
```

```
EMAIL_HOST_PASSWORD
ваш-пароль-приложения
```

```
EMAIL_PORT
465
```

```
EMAIL_USE_SSL
true
```

```
DEFAULT_FROM_EMAIL
ваш-email@yandex.ru
```

⚠️ Для Yandex нужно создать пароль приложения в настройках безопасности.

---

## Шаг 10: Настройка автоматического деплоя

### 10.1 Проверьте настройки
1. Перейдите в **"Settings"**
2. Найдите раздел **"Deploy Triggers"**
3. Убедитесь, что включен автодеплой для вашей ветки

Теперь при каждом `git push` Railway автоматически задеплоит изменения!

---

## Полезные команды Railway CLI

### Просмотр логов
```bash
railway logs
```

### Выполнение команд Django
```bash
railway run python UseMyTime/manage.py <команда>
```

Примеры:
```bash
# Применить миграции
railway run python UseMyTime/manage.py migrate

# Собрать статику
railway run python UseMyTime/manage.py collectstatic --no-input

# Создать дамп базы данных
railway run python UseMyTime/manage.py dumpdata > backup.json

# Загрузить данные
railway run python UseMyTime/manage.py loaddata backup.json
```

### Подключение к базе данных
```bash
railway connect postgres
```

### Просмотр переменных окружения
```bash
railway variables
```

---

## Troubleshooting (Решение проблем)

### Проблема: Ошибка 500 на сайте

**Решение:**
1. Проверьте логи: `railway logs`
2. Убедитесь, что `DEBUG=False`
3. Проверьте, что все переменные окружения настроены
4. Проверьте `ALLOWED_HOSTS` и `CSRF_TRUSTED_ORIGINS`

### Проблема: Статические файлы не загружаются

**Решение:**
```bash
railway run python UseMyTime/manage.py collectstatic --no-input
```

### Проблема: Ошибка подключения к базе данных

**Решение:**
1. Проверьте, что PostgreSQL сервис запущен
2. Проверьте переменные `DB_*` в настройках
3. Убедитесь, что используете правильные ссылки на переменные PostgreSQL

### Проблема: Миграции не применяются

**Решение:**
```bash
railway run python UseMyTime/manage.py migrate
```

### Проблема: Загруженные файлы (media) исчезают

**Причина:** Railway использует эфемерную файловую систему.

**Решение:** Используйте внешнее хранилище:
- AWS S3
- Cloudinary
- Railway Volumes (платная функция)

---

## Мониторинг и метрики

### Просмотр метрик
1. Откройте ваш сервис в Railway
2. Перейдите на вкладку **"Metrics"**
3. Смотрите:
   - CPU usage
   - Memory usage
   - Network traffic

### Настройка алертов
1. Перейдите в **"Settings"**
2. Найдите раздел **"Alerts"**
3. Настройте уведомления при проблемах

---

## Стоимость

### Бесплатный план
- $5 бесплатных кредитов в месяц
- Достаточно для тестирования

### Примерная стоимость
Для небольшого Django проекта:
- **$5-10/месяц** при умеренной нагрузке
- **$15-20/месяц** при активном использовании

### Оптимизация расходов
1. Используйте sleep mode для dev окружений
2. Оптимизируйте запросы к БД
3. Используйте кэширование
4. Мониторьте использование ресурсов

---

## Резервное копирование

### Создание бэкапа базы данных
```bash
# Экспорт данных
railway run python UseMyTime/manage.py dumpdata > backup_$(date +%Y%m%d).json

# Экспорт только определенных приложений
railway run python UseMyTime/manage.py dumpdata accounts projects > backup.json
```

### Восстановление из бэкапа
```bash
railway run python UseMyTime/manage.py loaddata backup.json
```

### Автоматический бэкап PostgreSQL
Railway автоматически делает бэкапы PostgreSQL базы данных.

---

## Следующие шаги

### 1. Настройте кастомный домен (опционально)
1. Купите домен (например, на Namecheap, GoDaddy)
2. В Railway: Settings → Domains → Add Custom Domain
3. Настройте DNS записи у регистратора домена

### 2. Настройте SSL (автоматически)
Railway автоматически настраивает SSL сертификаты для всех доменов.

### 3. Настройте мониторинг
Используйте сервисы:
- Sentry (для отслеживания ошибок)
- UptimeRobot (для мониторинга доступности)

### 4. Настройте CI/CD
Railway уже предоставляет автоматический деплой из GitHub.

---

## Полезные ссылки

- 📖 [Railway Documentation](https://docs.railway.app)
- 🐍 [Django Deployment Checklist](https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/)
- 💬 [Railway Discord Community](https://discord.gg/railway)
- 📧 [Railway Support](https://railway.app/help)

---

## Чеклист финальной проверки

- [ ] Сайт открывается по домену
- [ ] Админка работает
- [ ] Статические файлы загружаются
- [ ] Можно войти в систему
- [ ] Можно создать проект
- [ ] Можно добавить задачу
- [ ] Email отправляется (если настроен)
- [ ] Нет ошибок в логах
- [ ] Создан суперпользователь
- [ ] Настроены переменные окружения
- [ ] DEBUG=False в продакшене

---

**Поздравляю! Ваш проект UseMyTime успешно задеплоен на Railway! 🎉**

Если возникнут вопросы - проверьте логи командой `railway logs` или напишите мне.
