# 🚀 Быстрый старт деплоя на Railway

## 1. Подготовка (уже сделано ✅)
- ✅ Создан `railway.toml`
- ✅ Создан `nixpacks.toml`
- ✅ Обновлен `Procfile`
- ✅ Добавлен `whitenoise` для статики
- ✅ Настроен `settings.py` для продакшена

## 2. Деплой за 5 минут

### Шаг 1: Создайте проект на Railway
1. Откройте https://railway.app
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий

### Шаг 2: Добавьте PostgreSQL
1. В проекте нажмите **"New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст базу данных

### Шаг 3: Настройте переменные окружения
Перейдите в **"Variables"** и добавьте:

```env
SECRET_KEY=django-insecure-ЗАМЕНИТЕ-НА-НОВЫЙ-КЛЮЧ
DEBUG=False
```

Railway автоматически добавит переменные PostgreSQL:
- `PGHOST`
- `PGPORT`
- `PGDATABASE`
- `PGUSER`
- `PGPASSWORD`

Добавьте маппинг для Django:
```env
DB_HOST=${{PGHOST}}
DB_PORT=${{PGPORT}}
DB_NAME=${{PGDATABASE}}
DB_USER=${{PGUSER}}
DB_PASSWORD=${{PGPASSWORD}}
DB_ENGINE=postgresql
```

### Шаг 4: Настройте домен
1. После деплоя Railway даст вам домен: `your-app.up.railway.app`
2. Добавьте его в переменные:
```env
ALLOWED_HOSTS=your-app.up.railway.app
CSRF_TRUSTED_ORIGINS=https://your-app.up.railway.app
```

### Шаг 5: Деплой!
Railway автоматически начнет деплой. Следите за процессом в разделе **"Deployments"**.

## 3. После деплоя

### Создайте суперпользователя
```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите
railway login

# Подключитесь к проекту
railway link

# Создайте суперпользователя
railway run python UseMyTime/manage.py createsuperuser
```

### Проверьте работу
1. Откройте ваш домен: `https://your-app.up.railway.app`
2. Войдите в админку: `https://your-app.up.railway.app/admin/`

## 4. Полезные команды

```bash
# Просмотр логов
railway logs

# Выполнение команд Django
railway run python UseMyTime/manage.py <command>

# Просмотр переменных
railway variables

# Подключение к базе данных
railway connect postgres
```

## 5. Автоматический деплой

Railway автоматически деплоит при каждом push в главную ветку GitHub.

## 6. Стоимость

- **$5** бесплатных кредитов в месяц
- Примерная стоимость: **$5-10/месяц** для небольшого проекта

## Troubleshooting

### Ошибка 500
Проверьте логи: `railway logs`

### Статические файлы не загружаются
```bash
railway run python UseMyTime/manage.py collectstatic --no-input
```

### Ошибка подключения к БД
Проверьте переменные окружения в разделе "Variables"

---

📖 Подробная инструкция: см. `DEPLOYMENT.md`
