# 🔧 Отладка проблем Railway

## Ошибка: "Application failed to respond"

Эта ошибка означает, что приложение не отвечает на запросы. Причины могут быть разные.

### Шаг 1: Проверьте логи

1. Откройте Railway Dashboard
2. Выберите ваш проект
3. Кликните на Django сервис
4. Перейдите на вкладку **"Deployments"**
5. Выберите последний деплой
6. Нажмите **"View Logs"**

Ищите ошибки (строки с ERROR, CRITICAL, Exception).

### Шаг 2: Проверьте переменные окружения

Перейдите на вкладку **"Variables"** и убедитесь, что установлены:

#### Обязательные переменные:

```env
SECRET_KEY=ваш-секретный-ключ-минимум-50-символов
DEBUG=False
DB_ENGINE=postgresql
```

#### Переменные базы данных:

Если у вас PostgreSQL сервис называется "Postgres":
```env
DB_HOST=${{Postgres.PGHOST}}
DB_PORT=${{Postgres.PGPORT}}
DB_NAME=${{Postgres.PGDATABASE}}
DB_USER=${{Postgres.PGUSER}}
DB_PASSWORD=${{Postgres.PGPASSWORD}}
```

⚠️ **ВАЖНО:** Замените `Postgres` на точное имя вашего PostgreSQL сервиса!

Чтобы узнать имя:
1. Кликните на PostgreSQL сервис
2. Посмотрите имя вверху страницы

#### Переменные домена (добавьте после получения домена):

```env
ALLOWED_HOSTS=ваш-домен.up.railway.app
CSRF_TRUSTED_ORIGINS=https://ваш-домен.up.railway.app
```

### Шаг 3: Типичные ошибки и решения

#### Ошибка 1: "No module named 'wsgi'"

**Причина:** Gunicorn не может найти wsgi.py

**Решение:** Убедитесь, что файл `UseMyTime/wsgi.py` существует

#### Ошибка 2: "django.core.exceptions.ImproperlyConfigured: Set the SECRET_KEY"

**Причина:** Не установлена переменная SECRET_KEY

**Решение:** 
1. Сгенерируйте ключ:
```python
from django.core.management.utils import get_random_secret_key
print(get_random_secret_key())
```
2. Добавьте в Variables: `SECRET_KEY=сгенерированный-ключ`

#### Ошибка 3: "FATAL: password authentication failed"

**Причина:** Неправильные данные для подключения к PostgreSQL

**Решение:**
1. Проверьте, что PostgreSQL сервис запущен
2. Проверьте переменные `DB_*`
3. Убедитесь, что используете правильные ссылки: `${{Postgres.PGHOST}}`

#### Ошибка 4: "DisallowedHost at /"

**Причина:** Домен не добавлен в ALLOWED_HOSTS

**Решение:**
1. Получите ваш домен Railway (Settings → Domains → Generate Domain)
2. Добавьте переменную: `ALLOWED_HOSTS=ваш-домен.up.railway.app`
3. Перезапустите деплой

#### Ошибка 5: "Port $PORT is already in use"

**Причина:** Gunicorn пытается использовать занятый порт

**Решение:** Railway автоматически устанавливает переменную PORT. Убедитесь, что в команде запуска используется `$PORT`

### Шаг 4: Проверка конфигурации локально

Запустите скрипт проверки:

```bash
# Установите переменные окружения локально
export SECRET_KEY="test-key"
export DEBUG="False"
export DB_ENGINE="postgresql"
export DB_HOST="localhost"
export DB_PORT="5432"
export DB_NAME="test"
export DB_USER="postgres"
export DB_PASSWORD="password"

# Запустите проверку
python check_railway_config.py
```

### Шаг 5: Тестирование локально с теми же настройками

```bash
# Установите те же переменные, что и на Railway
export SECRET_KEY="ваш-ключ-с-railway"
export DEBUG="False"
export DB_ENGINE="postgresql"
# ... остальные переменные

# Запустите сервер
cd UseMyTime
python manage.py runserver
```

Если работает локально, но не на Railway - проблема в конфигурации Railway.

### Шаг 6: Используйте Railway CLI для отладки

```bash
# Установите CLI
npm install -g @railway/cli

# Войдите
railway login

# Подключитесь к проекту
railway link

# Просмотрите логи в реальном времени
railway logs --follow

# Проверьте переменные
railway variables

# Запустите команду в Railway окружении
railway run python check_railway_config.py

# Проверьте Django
railway run python UseMyTime/manage.py check --deploy
```

### Шаг 7: Принудительный перезапуск

1. В Railway Dashboard перейдите на вкладку **"Deployments"**
2. Нажмите **"Redeploy"**
3. Следите за логами

### Шаг 8: Проверка здоровья приложения

После успешного деплоя проверьте:

```bash
# Проверьте, что сервер отвечает
curl https://ваш-домен.up.railway.app

# Проверьте админку
curl https://ваш-домен.up.railway.app/admin/

# Проверьте статические файлы
curl https://ваш-домен.up.railway.app/static/
```

## Частые проблемы по категориям

### База данных

**Симптом:** Ошибки подключения к БД

**Чеклист:**
- [ ] PostgreSQL сервис запущен
- [ ] Переменные `DB_*` настроены
- [ ] Используются правильные ссылки на переменные PostgreSQL
- [ ] Миграции применены

**Команды для проверки:**
```bash
railway run python UseMyTime/manage.py dbshell
railway run python UseMyTime/manage.py migrate --check
```

### Статические файлы

**Симптом:** CSS/JS не загружаются

**Чеклист:**
- [ ] Whitenoise установлен
- [ ] collectstatic выполняется при деплое
- [ ] STATIC_ROOT настроен

**Команды для проверки:**
```bash
railway run python UseMyTime/manage.py collectstatic --dry-run
railway run python UseMyTime/manage.py findstatic admin/css/base.css
```

### Переменные окружения

**Симптом:** Приложение не видит переменные

**Решение:**
1. Проверьте, что переменные добавлены в правильный сервис (Django, не PostgreSQL)
2. После добавления переменных Railway автоматически перезапускает сервис
3. Проверьте логи на наличие переменных

### Порт

**Симптом:** "Failed to bind to port"

**Решение:**
Railway автоматически устанавливает переменную `PORT`. Убедитесь:
- Gunicorn использует `--bind 0.0.0.0:$PORT`
- Не хардкодите порт (не используйте 8000, 3000 и т.д.)

## Полезные команды Railway CLI

```bash
# Просмотр всех переменных
railway variables

# Добавление переменной
railway variables set SECRET_KEY="новый-ключ"

# Удаление переменной
railway variables delete OLD_VAR

# Просмотр логов
railway logs
railway logs --follow  # в реальном времени

# Выполнение команд
railway run <команда>

# Подключение к базе данных
railway connect postgres

# Информация о проекте
railway status

# Открыть проект в браузере
railway open
```

## Контрольный список перед обращением в поддержку

- [ ] Проверил логи деплоя
- [ ] Проверил все переменные окружения
- [ ] PostgreSQL сервис запущен
- [ ] Домен добавлен в ALLOWED_HOSTS
- [ ] Локально приложение работает
- [ ] Запустил `railway run python check_railway_config.py`
- [ ] Попробовал перезапустить деплой
- [ ] Проверил, что используется правильная ветка GitHub

## Получение помощи

Если ничего не помогло:

1. **Railway Discord:** https://discord.gg/railway
2. **Railway Help Station:** https://railway.app/help
3. **Railway Docs:** https://docs.railway.app

При обращении в поддержку предоставьте:
- Логи деплоя (последние 50-100 строк)
- Список переменных окружения (без паролей!)
- Описание проблемы
- Что уже пробовали

---

**Совет:** Большинство проблем решается правильной настройкой переменных окружения!
