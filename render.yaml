services:
  # Web Service
  - type: web
    name: UseMyTime
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: UseMyTime.settings
    autoDeploy: true
